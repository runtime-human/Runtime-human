# Repository source of truth

The private GitHub repository `MrFr3di/Runtime-human` is the only canonical storage for architecture, ADR, schemas, plans, code and content.

Files generated for download in chat are exports and do not override repository documents. Any accepted change must be committed to Git and linked from the documentation index.