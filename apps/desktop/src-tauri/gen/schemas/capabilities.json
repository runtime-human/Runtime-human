{
  "default": {
    "identifier": "default",
    "description": "Minimal permissions for the main Runtime Human window",
    "local": true,
    "windows": ["main"],
    "permissions": ["core:default"]
  }
}
