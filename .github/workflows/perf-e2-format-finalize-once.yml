name: perf-e2-format-finalize-once

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

concurrency:
  group: perf-e2-format-finalize-${{ github.event.pull_request.head.ref }}
  cancel-in-progress: true

permissions:
  contents: write

defaults:
  run:
    shell: pwsh

jobs:
  finalize:
    if: >-
      github.actor != 'github-actions[bot]' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'agent/perf-02a-windows-capture-harness'
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          clean: true
          persist-credentials: true
          show-progress: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "24"
      - name: Activate pnpm and select PowerShell script shell
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          corepack enable
          corepack prepare pnpm@11.11.0 --activate
          $pwshCommand = Get-Command pwsh -ErrorAction Stop
          "PNPM_CONFIG_SCRIPT_SHELL=$($pwshCommand.Source)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          pnpm --version
      - name: Install frozen dependencies
        run: pnpm install --frozen-lockfile --reporter=silent
      - name: Materialize canonical repository formatting
        run: pnpm fmt
      - name: Verify canonical repository formatting
        run: pnpm fmt:check
      - name: Commit only formatted tests and remove helper
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          $allowedTests = @(
            "tests/desktop-evidence-directory-contract.test.ts",
            "tests/desktop-evidence-runtime-contract.test.ts"
          )
          $changed = @(
            @(git diff --name-only)
            @(git ls-files --others --exclude-standard)
          ) | Where-Object { -not [string]::IsNullOrWhiteSpace($_) } | Sort-Object -Unique
          $unexpectedBeforeCleanup = @($changed | Where-Object { $_ -notin $allowedTests })
          if ($unexpectedBeforeCleanup.Count -gt 0) {
            throw "Unexpected formatter changes: $($unexpectedBeforeCleanup -join ', ')"
          }
          Remove-Item ".github/workflows/perf-e2-format-finalize-once.yml" -Force
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- `
            tests/desktop-evidence-directory-contract.test.ts `
            tests/desktop-evidence-runtime-contract.test.ts `
            .github/workflows/perf-e2-format-finalize-once.yml
          $staged = @(git diff --name-only --cached)
          $allowed = @(
            "tests/desktop-evidence-directory-contract.test.ts",
            "tests/desktop-evidence-runtime-contract.test.ts",
            ".github/workflows/perf-e2-format-finalize-once.yml"
          )
          $unexpected = @($staged | Where-Object { $_ -notin $allowed })
          if ($unexpected.Count -gt 0) {
            throw "Unexpected staged formatter changes: $($unexpected -join ', ')"
          }
          if ($staged.Count -eq 0) {
            throw "Formatter finalizer produced no test or helper-removal diff"
          }
          git diff --cached --check
          git commit -m "style: format PERF evidence contracts"
          git push origin HEAD:agent/perf-02a-windows-capture-harness
